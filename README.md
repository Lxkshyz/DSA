# DSA

### Day 1 Progress

- Learned what **Nodes** are and how they store data + pointer to next node
- Understood different types of Linked Lists:
    - **Singly Linked List**
    - **Doubly Linked List**
    - **Circular Linked List**
    - **Doubly Circular Linked List**
- Implemented a **basic Singly Linked List** with:
    - `add()` to insert at the end
    - `insert(index, data)` to add anywhere in the list
    - `delete()` to remove the last element
    - `getHead()`, `getTail()`, and `getSize()`
    - proper `toString()` to display the list
